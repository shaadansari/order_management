# --- Web framework ---
fastapi
uvicorn[standard]

# --- DB / ORM ---
sqlalchemy>=2.0

# --- Validation / settings ---
pydantic>=2
pydantic-settings
email-validator            # required by Pydantic's EmailStr

# --- Auth ---
PyJWT
bcrypt

# --- Misc ---
python-multipart           # form parsing for OAuth2 password flow in /docs

# --- Caching ---
redis==5.0.1              # in-memory cache in front of GET /v1/products

# --- Tests ---
pytest
httpx                      # FastAPI TestClient transport
